import { styled } from "@mui/joy/styles";
import { Box } from "@mui/joy";

export const NoteHeader = styled(Box)(() => ({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
}));
